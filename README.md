# AddressBook

## Docker Setup (Address Book API)

**Clone the project**  
   ```bash
   git clone https://github.com/your-repo/address-book.git
   cd address-book

   docker compose up -d --build
   ```
**Open Swagger UI**
   http://localhost:8000/docs

